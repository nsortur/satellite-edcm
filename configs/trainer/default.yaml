_target_: pytorch_lightning.Trainer

accelerator: "auto"
devices: "auto"
max_epochs: 100
precision: 32

# change after done debugging with small dataset
check_val_every_n_epoch: 1
log_every_n_steps: 1
